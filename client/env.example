# Configuração para Cloudflare
# Copie este arquivo para .env.local e configure as variáveis

# URL do servidor WebSocket (Worker)
VITE_SERVER_URL=wss://skyoffice-server.your-subdomain.workers.dev

# Ambiente
NODE_ENV=production
